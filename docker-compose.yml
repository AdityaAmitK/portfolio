services:
  portfolio:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_PATH: /app/data/portfolio.db
    volumes:
      - portfolio_data:/app/data
    ports:
      - "127.0.0.1:3000:3000"

volumes:
  portfolio_data:
